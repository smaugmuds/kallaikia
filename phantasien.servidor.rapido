#!/bin/bash

# Cargar configuracion
source phantasien.servidor.configuracion

echo
echo "          - (( $programa $version )) -"
echo
echo "           \"$reino\""
echo

sleep 2;

# Cambiar al directorio del reino | zonas
cd $reino

# Ejecutar el servicio
nohup "../$programa $puerto 2> $logservidor" > ../log/$programa.servidor.rapido.log &

