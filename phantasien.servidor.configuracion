#
# Phantasien (Reinos de desesperacion)
#

programa=phantasien
reino="reinos_de_desesperacion"
#reino="realms_of_despair"

version="1.8.3a"

# Configuración del servicio

servidor="localhost"
puerto="4000"

# Fichero de log principal

# Los logs se configuran en el directorio ../logs,
# por que el $programa principal se ejecuta desde el directorio del $reino,
# donde estan las zonas.

logservidor="../log/phantasienIII.log"
